@Contents
$f4 帮助索引 (版本: %Ver)
f4 是一个用 Go 编写的实验性 Far Manager / far2l 克隆版。

~查看器 / 编辑器键盘快捷键~ViewerEditor@
~面板导航~PanelNav@
~可视化文件重命名 (VisRen)~VisRen@
~查看 README.md~README@

@VisRen
$可视化文件重命名 (VisRen)

VisRen 是 F4 内置的原生可视化批量重命名工具。它在进行任何更改之前构建
每个新文件名，并并排显示源文件名和目标文件名。它适用于本地文件系统面板
上的文件和目录。

如果面板项目已被选中，VisRen 会按面板顺序打开它们。如果未选中任何项目，
则会使用面板光标下的项目。永远不会使用上级目录 ".."。按 F11 并选择
"Visual File Renamer" 以打开 VisRen；分配的菜单热键也可以激活 it。

~快速入门和对话框字段~VisRenQuickStart@
~名称掩码、扩展名、范围、计数器和转义~VisRenMasks@
~大小写转换、转译和处理顺序~VisRenTransforms@
~日期、ID3、图像/EXIF 和可执行文件元数据~VisRenMetadata@
~搜索、普通替换和正则表达式~VisRenSearch@
~预览导航、键盘和鼠标参考~VisRenPreview@
~在 UTF-8 编辑器中编辑重命名列表~VisRenEditor@
~重命名执行、错误、日志和撤销~VisRenRename@
~便携式文件名规则和重要限制~VisRenSafety@
~实用技巧实例~VisRenExamples@

该实现遵循 FarPlugins VisRen 3.2.0，具有更严格的文件名和编辑器验证、
稳定的随机值以及便携式元数据。

@VisRenQuickStart
$VisRen: 快速入门和对话框字段

#典型的重命名流程#

  1. 使用 Ins 键标记所需的文件/目录，或将面板光标放在一个项目上
     (如果只需要该项目)。
  2. 按 F11 并选择 VisRen。
  3. 输入名称和扩展名的掩码。默认值 [N] 和 [E] 保持两部分不变。
  4. 选择性输入 "查找" 和 "替换为" 文本。焦点始于 "查找"; 按 Tab 键
     可直接移至 "替换为"。
  5. 检查每个目标路径。按 F3 显示当前行的完整源文件名、目标文件名或
     诊断错误。
  6. 选择 "重命名" 或在默认按钮上按回车键。

#名称和扩展名#

这两个字段分别构建目标基本名称和扩展名。扩展名字段不包含圆点。
空扩展名结果将完全移除扩展名。字面量文本将按原样复制; 括号表达式则
会插入源文本、计数器、元数据或进行大小写转换。

最后一个圆点分割源文件名和扩展名。例如 archive.tar.gz 的名称为 archive.tar，
扩展名为 gz。README 没有扩展名。以圆点开头的文件(如 .gitignore)的名称为空，
扩展名为 gitignore。

#模板和 [+]#

名称模板和扩展名模板列表包含对相应掩码有效的表达式。选择条目仅代表选中
它。点击 [+] 按钮会将选定的模板插入在光标处，并将焦点返回到相应的掩码，
这与原版 VisRen 相同。当模板列表获得焦点时，按 Ins 键可以插入其当前选中
的条目，同时将焦点保持在列表中。插入时，范围和计数器占位符将转换为真实值。

#搜索控制#

搜索和替换是在两个掩码构建出完整的文件名后执行的。默认情况下已启用
"区分大小写"。"正则表达式"可在字面查找和 Regex 模式之间切换。详情请参见
"搜索"主题。

#实时预览和诊断#

每次编辑都会实时更新预览。如果掩码、范围、计数器、正则表达式或生成的文件名
无效，所有目标都会显示为 <Error!>。此时重命名和 F4 编辑器将被禁用。按
F3 可查看实际的诊断信息。

这四个编辑字段使用 F4 的历史记录。用于重命名的值会被添加至其历史记录中。
[T] 的单词分隔符单独保存在 VisRen 用户配置中。

按退格键 (Backspace) 返回至 VisRen 帮助索引。

@VisRenMasks
$VisRen: 掩码、范围、计数器和转义

#基本表达式#

  [N]          不带最后一个扩展名的原始名称。
  [E]          原始扩展名(不带前导圆点)。
  [[]          字面量左方括号。
  []]          字面量右方括号。

表达式之外的所有文本都是字面量。孤立的右方括号、未闭合的左方括号
或未知的表达式都将被视为错误。

#范围#

范围跟在 N 或 E 的后面。位置计算的是 Unicode 字符数，而非字节，且从 1 开始
计数。负位置表示从结尾向前倒计数。相同的格式也适用于 E，例如 [E1-3]。

对于源名称 abcdefghij:

  [N2]         b                 一个字符
  [N2-5]       bcde              第 2 到第 5 个字符(含)
  [N2,5]       bcdef             从第 2 个字符开始的五个字符
  [N2-]        bcdefghij         第 2 个字符cell直至末尾
  [N-2]        i                 倒数第二个字符
  [N2--5]      bcdef             从第 2 个字符到倒数第五个字符
  [N-8-5]      cde               倒数第八个到倒数第五个
  [N-8,5]      cdefg             从倒数第八个字符开始的五个字符
  [N-5-]       fghij             从倒数第五个字符开始到末尾

超出范围的部分会尽可能被截断或产生空结果。位置 0 和反向范围是错误。
下拉列表中的范围模板会使用最长的源名称或扩展名。

#计数器#

  [C1]         1, 2, 3, ...
  [C001]       001, 002, 003, ...
  [C001+5]     001, 006, 011, ...
  [C10+-2]     10, 08, 06, ...

初始数字决定了第一个值和最小显示宽度。它们最多可包含九位数字。如果省略了
+步长，则步长默认为 1。计数器遵循预览顺序，因此使用 Ctrl+Up/Down 或拖放
操作重新排列行时会更改分配的值。宽度为最小值：较大的值不会被截断。

#组合表达式#

表达式和字面量可以自由组合。例如，[C001+1]_[N1-12] 会生成一个三位数的序号、
一个下划线以及原文件名前十二个字符。转换表达式不添加任何文本，它们会按
"大小写转换"主题所述修改对应的部分。

按退格键返回至 VisRen 帮助索引。

@VisRenTransforms
$VisRen: 大小写转换、转译和处理顺序

#大小写转换#

  [L]          将整个掩码结果转换为小写。
  [U]          将整个掩码结果转换为大写。
  [F]          首字母大写，其他字母小写。
  [T]          每个单词的首字母大写。
  [M]          格式化为 "歌手 - 歌曲标题" 风格的音乐名称。

[T] 默认使用 "-. _&" 作为单词分隔符。选择任意模板列表中的 [T]，然后按 F4
即可编辑这些分隔字符。VisRen 配置中最多可保存 18 个字符并于日后重复使用。

[M] 可以识别歌手与歌曲标题之间的 " - " 或 "_-_"。歌手名称中的每个单词都会
首字母大写; 歌曲标题仅首个字母大写，其他部分均为小写。歌手名中孤立的俄语
连词 "и" 会被小写，这与原版 VisRen 的行为一致。

如果在一个掩码中出现多个大小写表达式，其优先级按以下顺序固定：[L], [U],
[F], [T], [M]。仅应用优先级最高的一个。

#转译#

  [TL]         将俄语 cyrilic 转译为 VisRen 拉丁拼写。
  [TR]         将 VisRen 拉丁拼写转换回俄语 cyrilic。

对应关系采用原版 VisRen 映射表：例如 ё -> jo, ж -> zh, х -> kh, щ -> shh,
ю -> ju, я -> ja。字符大小写尽可能保留。反向转换会选择最长的匹配拉丁序列。
如果 [TL] 和 [TR] 同时存在，[TL] 优先。

#精确的处理顺序#

  1. 展开名称和扩展名表达式。
  2. 如果扩展名非空，用圆点将两部分连接起来。
  3. 将查找/替换应用于此生成的完整文件名。
  4. 将结果在最后一个圆点处重新切分。
  5. 对请求了转译的掩码部分应用转译。
  6. 最后应用选定的大小写转换。
  7. 验证最终生成的单个文件名。

这个顺序很重要。大小写转换也会影响由查找/替换插入的替换文本，而搜索则可
能匹配到新生成的扩展名部分。

按退格键返回至 VisRen 帮助索引。

@VisRenMetadata
$VisRen: 日期和文件元数据

元数据在读取时是延迟(lazy)加载的，且对每个源项目仅缓存一次。缺失或不支
持的字段将展开为空字符串，不会使预览中断。

#文件时间戳和会话值#

  [DM]         本地时间下的最后修改日期：YYYY.MM.DD
  [TM]         本地时间下的最后修改时间：hh-mm-ss
  [R]          从 0 到 32767 之间的随机十进制数

[R] 在对话框打开时分配一次。在编辑掩码时它保持稳定，并在重排预览行时跟随
源项目。新的 VisRen 会话将分配新的随机值。

#MP3 / ID3#

音轨编号表达式是左括号、ASCII 井号 (U+0023) 和右括号。其他表达式为：

  [t]          音轨标题
  [a]          艺术家
  [l]          专辑名
  [y]          年份/日期
  [g]          流派

当 ID3v2 标签存在时，优先读取 ID3v2.3 和 ID3v2.4 标签。否则使用 ID3v1.1，
包括其音轨编号和流派对照表。支持的编码包括系统的 Legacy 编码、UTF-8
和 UTF-16。

#图像和 EXIF#

  [c]          JPEG EXIF 相机制造商
  [m]          JPEG EXIF 相机型号
  [d]          拍摄日期：YYYY.MM.DD hh-mm-ss
  [r]          像素尺寸表示为 宽度x高度

像素尺寸支持 JPEG, BMP, GIF 和 PNG。相机制造商、型号和拍摄日期自 JPEG 的
EXIF 字段中读取。若不含 EXIF 日期，则 [d] 会使用文件的最后修改时间。请注
意，小写的 [m]/[r] 是元数据占位符，而大写的 [M]/[R] 则是转换和随机值表
达式。

#程序版本#

  [V]          Windows PE .exe 或 .dll 资源中的固定的文件版本号

PE 版本数据在所有受支持的系统上都会进行原生解析。无可用版本信息的文件会
将 [V] 展开为空字符串。

包含路径分隔符或其他无效字符的元数据文本将被丢弃，或在最终文件名验证时
被拦截。在处理不受您控制的文件标签时，请务必仔细检查预览结果。

按退格键返回至 VisRen 帮助索引。

@VisRenSearch
$VisRen: 搜索、替换和正则表达式

搜索和替换作用于由两个掩码构建出的完整文件名(含生成的圆点及扩展名)。
"查找"字段为空时，该阶段将被禁用。每个不重叠的匹配项都会被替换。

#字面量普通模式#

当未勾选 "正则表达式" 时，"查找" 表示字面量文本。当勾选 "区分大小写"
时，"File" 和 "file" 会被视作不同。若未勾选，将采用小写比较，替换文本则
按原样插入。

示例：掩码 [N] 和 [E], "查找" 填入 "draft_", 替换为空，可将
draft_report.txt 转换为 report.txt。

#正则表达式模式#

勾选 "正则表达式" 以调用 F4 基于 Go/coregex 的高能正则引擎。可以直接输入
表达式，或写成 /pattern/i 格式。在带斜杠的格式下，要表示字面量斜杠请写
成 \/。可选的 i 标志可忽略大小写。取消勾选 "区分大小写" 能达到相同效果。
不接受除 i 以外的其他 Suffix 标志。

替换变量使用 $0表示完整匹配，使用 $1 到 $9 表示捕获括号组。例如对于
001-report.txt:

  查找:             /(\d+)-([^.]+)/
  替换为:           $2-$1
  结果:             report-001.txt

正则模式有误、未闭合 of /pattern/ 或不支持的标志都将使预览显示为 <Error!>，
重命名和编辑器将被禁用。可以通过 F3 查看详细的出错诊断。

按退格键返回至 VisRen 帮助索引。

在原始文件名中找到的匹配项将在左侧预览列中高亮显示。高亮显示会遵循 "区
分大小写" 和 "正则表达式" 的设置，标出所有可见的匹配项，并在滚动列时保
持对齐。由 "替换为" 产生的文本将在右侧列中高亮显示。对于正则表达式，
这包括完整的展开替换文本；如果替换为空，则无可见文本，不会高亮。

@VisRenPreview
$VisRen: 预览、键盘和鼠标

左侧预览列是源名称；右侧是目标。当对话框打开时，默认选中第一行，且键盘
焦点起始于 "查找" 字段。F12 可以将焦点移动到预览列表中，按 Namens 掩码
可以返回。

当列表行数超出预览窗口时，右侧将出现垂直滚动条。可以用鼠标点击箭头、滑
块或在列表上滚动滚轮来进行浏览。列表较短时，滚动条会自动隐藏。

#通用按键#

  F1                   打开此帮助页面。
  F2                   切换是否记录重命名日志。
  F3                   显示当前行的完整名称或错误诊断信息。
  F4                   在 F4 的编辑器中打开此列表。
  F4 (聚焦 [T] 模板时)  编辑 Title Case 的单词分隔符。
  F5                   最大化/恢复窗口大小。
  F6                   加载最近一次的撤销(Undo)数据。
  F8                   在确认后清除内存中的重命名日志。
  F12                  在预览列表和输入字段之间移动焦点。
  Alt+N                聚焦至名称掩码。
  Alt+E                聚焦至扩展名掩码。
  Alt+S                聚焦至 "查找" 输入框。
  Alt+P                聚焦至 "替换为" 输入框。
  Alt+C                切换 "区分大小写"。
  Alt+G                切换 "正则表达式"。
  Alt+T                聚焦至名称模板列表。
  Alt+L                聚焦至扩展名模板列表。
  回车 (Enter)         执行默认按钮对应的 "重命名" 操作。
  Esc                  关闭对话框，不执行任何重命名。

#聚焦在预览列表时可用的按键#

  Up/Down              移动选中行。
  Home/End             跳至第一行 / 最后一行。
  PgUp/PgDn            向上 / 向下滚动一页预览。
  Del                  从本次批量重命名中移除该行。
  Ctrl+Up/Down         重新排列行顺序; 计数器会按新顺序重新分配。
  Ctrl+Left/Right      移动左右两列的分隔线。
  Ctrl+Num5            使分隔线居中，并重置所有的滚动偏置。
  Alt+Left/Right       横向滚动左侧的源名称列。
  Left/Right           横向滚动右侧的目标名称列。
  Ctrl+PgUp            关闭 VisRen，返回活动面板上的该原文件。

#模板和鼠标操作#

  在模板列表上按 Ins 键可以将当前模板插入至对应的掩码中。
  按住鼠标左键并拖动预览行，可以进行重排。
  在预览行上右键单击可以查看其完整路径或当前的错误诊断。
  双击预览行会关闭 VisRen 并跳至活动面板上的该原文件上。
  点击标题栏上的缩放图标可最大化/恢复窗口。
  点击 "记录重命名日志" 选框可切换状态；点击其后面的文本并确认，
  可以清除已保存 of 日志。

调整终端大小或按 F5 键会将预览区域重置为各占 50% 宽度的对等布局。
手动调整分隔线或进行横向滚动后可以再次改变此布局。

按退格键返回至 VisRen 帮助索引。

@VisRenEditor
$VisRen: 在 UTF-8 编辑器中编辑列表

按 F4 键可以调出导出和编辑模式对话框，随后会新建一个临时的 UTF-8 文档，
标题为 "Rename list of files"，并在 F4 的编辑器中打开它：

  源名称 + 目标        "原文件名"    "新文件名"
  仅目标                             "新文件名"

每个值都用双引号包围。文件名中的双引号、反斜杠和控制字符都会采用转义的
字符串表示。请保持两端的外侧双引号完整。

#严格的验证#

当编辑器关闭时，VisRen 会要求进行以下严格检查：

  - 文件的行数必须与预览中的完全一致；
  - 每行必须恰好包含 1 个或 2 个被双引号包围的列(取决于选择的编辑模式)；
  - 双引号之外不能含有其他冗余文字；
  - 在两列模式下，原文件名那一列必须完全未被修改，且各行的顺序不变；
  - 每个目标文件名都必须是有效的便携式文件名组件。

请不要在编辑器中增加、删除或重排各行。在两列模式下，只允许修改第二列。
通过这些校验，因双引号格式写错或不小心改动了源文件的行可以被安全拦截，
不会导致直接写入。

如果发生错误，VisRen 会提示原因并提供 "重新编辑" (Edit again)。
编辑器会再次打开并定位在出错行上，同时保留您的修改。校验通过后，编辑器
关闭时会直接启动按序重命名。如果生成的列表完全没有发生字节改变，但含有
掩码修改，VisRen 也会先要求确认后才会运行。

在预览包含 <Error!> 或处于撤销预览状态时，编辑器功能将被禁用。

按退格键返回至 VisRen 帮助索引。

@VisRenRename
$VisRen: 执行、错误处理、日志和撤销

#顺序重命名#

重命名操作将严格按照预览中的顺序从上到下依次执行。没有发生名称更改的项目
将被直接跳过。如果源项目在打开对话框后、执行前已被其他程序删除，也会被
跳过。文件和文件夹采用相同的规则。

已经由其他项目占用的路径绝不会被覆盖。同一对象的纯大小写转换重命名是被
完美支持的，F4 在必要时会引入临时文件名中转。F4 内部并不会去计算多项目的
相互置换：例如 a -> b 且 b -> a 通常会直接触发目标文件已存在的错误，
这与原版 VisRen 的行为相同。

#运行错误#

当遇到缺少权限、目标已被占用或有重复、系统长度超限以及其他文件系统错误时，
系统会提示：

  重试 (Retry)         再次尝试当前这一行的重命名。
  跳过 (Skip)          保持该项目不变，继续下一个。
  全部跳过 (Skip all)   在后续出错时不再询问，直接跳过。
  取消 (Cancel)        停止操作；剩余的各行不予处理。

批量重命名并不是事务性的。即使在选择跳过或取消后，在此之前已经成功重命名
的项目会保持不变。失败和未被处理的项目在返回面板后，会保持在选中状态以便
日后再次处理。完成后的统计消息会报告总共成功重命名了多少个项目。

#重命名日志#

默认情况下已启用日志记录。勾选 "记录重命名日志" 表示成功的每一对重命名
都将在内存中保存记录。可以通过 F2 键或点击该复选框切换状态。日志不会写入
磁盘，且在退出 F4 时会直接丢失。它仅保存最近一次批量重命名的成功数据和
对应的执行目录；其后面的 * 号代表存在日志。

按 F8 或点击日志后面的文本并确认，可以清空已保存的日志。如果在日志记录被
禁用的状态下运行了重命名，则不会产生撤销数据。

#撤销 (F6)#

按 F6 键可以保存メモリ内の最後の一括リネームログをロードし、行を逆順に表示して
新しい名前を元の名前に戻します。ウィンドウのタイトルバーにログに対応するディレクトリが
表示されます。リネームを選択して元に戻すことを確認するか、メッセージが表示されたときに
No を選択して通常のリネーム状態に戻ります。

撤销(Undo)同样遵守无覆盖和错误提示规则。它在开始前会先切换至保存の実行
ディレクトリにまず切り替えます。元に戻す作業がすべて成功しても一部だけであっても、
作業が終われば該当のログはクリアされ、元に戻すのに失敗した項目は引き続き選択状態を維持
します。

退格键返回至 VisRen 帮助索引。

@VisRenSafety
$VisRen: 文件名安全性と制限事項

変更されたターゲットファイル名は、有効なポータブルファイル名である必要があります。VisRen は
以下の名前を禁止します:

  - 空のファイル名、"."、".."。
  - 制御文字 (Unicode コード値が 0020 未満の文字、または NUL)。
  - <、>、:、"、*、?、/、バックスラッシュ、または垂直分割線。
  - 末尾のスペースまたはドット。
  - DOS システムで予約された特殊デバイス名: CON, PRN, AUX, NUL, COM1..COM9 および LPT1..LPT9
    （拡張子の有無、大文字小文字の区別を問わない）。

スラッシュ "/" を使用してファイルを別のフォルダへ移動することはできません。変更されない
元のファイル名は許可され、即座にスキップされます。該当の元のファイル名自体が、上記の
ポータブルな命名規則に合致していない場合でも許容されます。

プレビュー検証はほとんどのマスクエラーおよびポータブル名のエラーをフィルタリングしますが、
ターゲットパスを事前に占有したりロックしたりはしません。ターゲットがすでに存在したり、
生成された名前に重複があったり、権限不足、システム長さ制限、および相互の置換は実行中に
エラーを引き起こす可能性があります。安心してください。いかなるファイルも予期せず上書き
されることはありません。

メタデータが不足しているか破損している場合、空のテキストに展開されます。メタデータプレース
ホルダーを使用する際は、接続記号を合理的に使用し、常にプレビューを確認してください。
VisRen はトランザクションを作成せず、複数項目置換時に一時ファイル名を自動的には使用しません。

退格键返回至 VisRen 帮助索引。

@VisRenExamples
$VisRen: 実用的なレシピ例

#ファイルのシリアル番号付与と拡張子の規格化#

  名前マスク:       [C001+1]_[N]
  拡張子マスク:     [E][L]

001_Old name.jpg、002_Other name.jpg などのファイル名が生成されます。シリアル番号が
別の順序に従う必要がある場合は、あらかじめプレビューで行の順序を調整してください。

#固定幅ファイル名から有用な部分を切り出す#

  ソースファイル:     20260805_report_final.txt
  名前マスク:       [N10-21]
  拡張子マスク:     [E]
  結果:             report_final.txt

#固定の字面通り（リテラル）の接頭辞を削除#

  ダブルマスク:     [N] と [E]
  検索:             draft_
  置換:             (空)

#正規表現を利用してキャプチャグループを入れ替える#

  ソースファイル:     001-report.txt
  検索:             /(\d+)-([^.]+)/
  置換:             $2-$1
  結果:             report-001.txt

#タグ情報をベースに MP3 ファイル名を変更#

  名前マスク:       [a] - [t]
  拡張子マスク:     [E][L]

トラック番号プレフィックスはカウンタで生成するか、「日付とファイルのメタデータ」テーマ
で説明されている ID3 トラック表現式で生成できます。アーティストやタイトルタグが
欠落している場合は空に展開されるため、プレビューをしっかり確認してください。

#撮影日時とサイズをベースに写真名を変更#

  名前マスク:       [d]_[r]_[C001]
  拡張子マスク:     [E][L]

JPEG の場合は EXIF の撮影日時が優先され、他のサポート形式の [d] 表現式はファイルの
更新日時を使用します。

#文字転写（トランスリテレーション）とリテラルの角括弧を追加#

  名前マスク:       [[]archive[]]_[N][TL]
  拡張子マスク:     [E][L]

オリジナルの転写表を使用して、ソースファイル名 Пример.TXT は [archive]_Primer.txt に
変更されます。

退格键返回至 VisRen 帮助索引。

@Panels
$f4 ヘルプ目次 (バージョン: %Ver)
f4 は、Go で書かれた実験的な Far Manager / far2l クローンです。

~ビューア / エディタのキー~ViewerEditor@
~パネルナビゲーション~PanelNav@
~README.md を表示~README@

@ViewerEditor
$ビューアおよびエディタのキー
この部分は、現在登録されているグローバルアクションを基に動的に生成されます。

@ViewerNav
$导航键
编辑器:
  Ctrl+Left     - 移至上一个单词边界
  Ctrl+Right    - 移至下一个单词边界
  Ctrl+Shift+Left/Right - 选中整个单词
  Home/End      - 移至行首 / 行尾
  PgUp/PgDn     - 向上 / 向下翻页

查看器:
  Up/Down       - 向上 / 向下滚动一行
  PgUp/PgDn     - 向上 / 向下翻一页
  Home/End      - 跳至文件开头 / 末尾

@PanelNav
$面板导航
此部分内容是根据当前注册的全局动作动态生成的。

@ShellNav
$面板导航键
  Tab           - 切换当前活动面板
  Insert        - 选择 / 取消选择文件或文件夹
  Ctrl+Tab      - 在不同的工作区之间循环切换
  Ctrl+W        - 关闭当前工作区
  Ctrl+N        - 将当前活动面板克隆到新工作区中打开

@History
$命令历史记录 (Alt+F8)
    命令历史记录显示了先前执行过的命令列表。
除了方向键之外，还可以使用以下键盘快捷键：

  将选中的命令插入到命令行中                 Enter
  (鼠标左键点击效果相同)
  取消并关闭此对话框                         Esc, F10

  切换子串 / 前缀增量搜索模式                 F2
  输入任意字符以对当前列表进行过滤
  Backspace 可删除最后一个过滤字符

  删除选中的那一条历史项                     Shift+Del
  在进行确认后清除全部命令历史               Del

  将选中的命令复制到系统剪贴板               Ctrl+C, Ctrl+Ins

@HistoryFolders
$フォルダ履歴 (Alt+F12)
    フォルダ履歴は、以前に訪問したディレクトリのリストを表示します。
方向キーのほかに、以下のショートカットキーを使用できます:

  アクティブパネルで選択したフォルダに移動     Enter
  (マウスの左ボタンクリックも同様の効果)
  非アクティブパネルで選択したフォルダに移動   Shift+Enter
  選択したパスを直接現在のコマンドラインに挿入 Ctrl+Enter
  ダイアログをキャンセルして閉じる             Esc, F10

  部分一致 / 前方一致インクリメンタル検索切り替え F2
  任意の文字を入力して現在のリストをフィルタリング
  Backspace で最後のフィルタ文字を削除

  選択した過去のパス項目を削除                 Shift+Del
  確認のうえ、すべてのフォルダ履歴をクリア     Del
  ディスクに存在しない過去の無効パスを整理(確認必要) Ctrl+R

  選択したパスをシステムクリップボードにコピー Ctrl+C, Ctrl+Ins