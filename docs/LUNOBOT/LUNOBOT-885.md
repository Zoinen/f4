# Лунобот-2 (instance a721a6d1487257292ae00780 LNX): тикет #885

## Статус

- [x] Полностью прочитаны тело и переписка тикета; изучены связанные upstream-ссылки и текущие документы ConPTY в репозитории.
- [x] В `DISPATCH.md` зарегистрирована часть 1 из 2.
- [x] Зафиксировано решение автора: не применять эвристику склейки строк, а использовать согласованную современную пару `conpty.dll` + `OpenConsole.exe`.
- [x] У автора запрошены критерии первой атомарной части: архитектура, точный пакет и проверяемый путь F3/F4.
- [ ] Получены и зафиксированы критерии готовности.
- [x] Реализовано подключение согласованной пары к f4 отдельным PR: f4 загружает
  `ConptyCreate/Resize/ClosePseudoConsole` из локальной `conpty.dll`, а
  `OpenConsole.exe` проверяется рядом с ней до загрузки; системный
  `kernel32` fallback не используется.
- [x] GitHub Windows CI подтвердил сборку пары и запуск f4-тестов с пакетной DLL:
  run `34187866847`, Windows amd64/arm64 build и Windows amd64 native tests — green.
- [x] PR первой части `https://github.com/unxed/f4/pull/948` объединён в
  `main`; тикет оставлен открытым, пользовательская проверка F3/F4 ещё нужна.
- [x] Зарегистрирована часть 2 из 2: переход от обязательной поставки пары к
  ленивому получению ConPTY при первом обращении, по аналогии со схемами
  Colorer, и условное включение полноценного word wrap.
- [x] Новое направление и вопросы о точном источнике, кэше, offline-поведении
  и критерии надёжной поддержки запрошены у владельца в
  `https://github.com/unxed/f4/issues/885#issuecomment-5579572705`.
- [x] Владелец дополнительно уточнил, что ConPTY уже передаёт обычный вывод
  без искусственного CRLF при сохранённых флагах
  `ENABLE_VIRTUAL_TERMINAL_PROCESSING | ENABLE_PROCESSED_OUTPUT`, но путь
  `WriteConsoleOutputCharacterW` остаётся отдельным случаем: там не вызывается
  `SetWrapForced`.
- [ ] Получены и зафиксированы критерии готовности второй части.

## Решения и инварианты

- Нельзя восстанавливать логические строки эвристикой по ширине строки и CRLF: это может незаметно склеить настоящие строки или потерять данные.
- `conpty.dll` и `OpenConsole.exe` должны поставляться согласованной парой из одного проверенного пакета; подмена только одного файла недопустима.
- Для Windows amd64 и arm64 используется пакет
  `Microsoft.Windows.Console.ConPTY.1.25.260710002-preview.nupkg` из релиза
  `v1.25.1912.0`, SHA-256
  `05fe9b571ea4fb198f5012405cb39a132cf23eee50feaa496524c149b2502692`.
- В обычном запуске f4 ищет пару только рядом с собственным exe; нативные
  Windows-тесты устанавливают ту же пару в каталог пакета до запуска тестов.
- Поведение реального Windows и минимальная версия ОС должны быть подтверждены GitHub Windows CI; локальные сборки и тесты не запускаются.
- Документы `docs/CONPTY_LINE_WRAP_FINDINGS.md`, `docs/CONPTY_GATE_REQUIREMENTS.md` и `docs/CONPTY_NATIVE_TEST.md` описывают предпосылки и измерения, но сами по себе не доказывают интеграцию пары в f4.
- Для второй части нельзя заранее выбирать место кэша, URL пакета или
  fallback-поведение: эти решения должны быть подтверждены владельцем до
  изменения runtime-архитектуры.
- Проверка обычного текстового вывода не доказывает корректность вывода через
  `WriteConsoleOutputCharacterW`; эти два пути должны рассматриваться отдельно
  при формулировке критерия поддержки word wrap.

## Проверка

Локальные сборки и тесты не запускались по инструкции Лунобота-2. GitHub CI run
`34187866847` завершился успешно: сборки Windows amd64/arm64 и нативные
Windows amd64 tests прошли; отдельный manual probe остаётся доступен для
сравнения raw ConPTY-потока. Итоговую пользовательскую приёмку F3/F4 должен
подтвердить владелец тикета. Вторая часть находится на этапе уточнения
критериев; новая реализация пока не начиналась.
