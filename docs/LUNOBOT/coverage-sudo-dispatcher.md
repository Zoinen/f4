# Кастомная задача покрытия `vfs/sudo_dispatcher_unix.go`

## Задача

По §22 `LUNOBOT.md` выбран пакет с настоящей Unix-логикой: в отчёте Codecov для основной ветки `vfs/sudo_dispatcher_unix.go` имел 0% покрытия при 161 строке. Платформенные заглушки и UI-файлы сознательно не выбирались.

## Атомарный шаг

Добавлен Unix-тест `TestHandleSudoClientDispatchesFilesystemCommands`. Через настоящий Unix-сокет он проверяет обработку `Ping`, `Stat`, `ReadDir` (включая ссылку на каталог), `Open` с передачей файлового дескриптора, отказ для socket, `MkDir`, `Rename`, `SetAttributes`, `Remove`, ошибку отсутствующего пути и завершение после disconnect.

## Статус

Первый CI run `34314909209` нашёл два `errcheck` замечания на отложенное закрытие Unix listener и client; оба закрытия теперь явно обрабатывают ошибку. Исправленный run `34315347437` завершился успешно на всех обязательных build/test/race/lint/quality job’ах. Локальные сборка и тесты не запускались согласно инструкции Лунобота.
