# Состояние задачи BRANCHES.md

Дата проверки: 08-09-2026; актуальный `main`: `5c89af913c2d49c23065871d6e88fe5b55763f6c`.

## Цель

Добавить в корень репозитория проверяемый инвентарь веток и рабочих деревьев,
чтобы после слияния задачи не оставались незаметными и чтобы несколько
Луноботов не принимали одну и ту же ветку за активную работу.

## Решения и инварианты

- Источники состояния — текущий `main`, список PR GitHub, локальные ветки и
  `git worktree list`; отсутствие PR не доказывает, что ветка принадлежит
  текущему инстансу.
- Слитая ветка помечается как завершённая, но её удаление не выполняется
  автоматически: рабочая копия общая, а удаление может уничтожить незавершённую
  работу другого инстанса.
- `BRANCHES.md` проверяется вместе с `CI.md` после каждого коммита в `main`.

## Текущее состояние

Корневой `BRANCHES.md` добавлен этим коммитом. Production-код не меняется;
на момент подготовки списка в `CI.md` нет незавершённых прогонов.

## Последующее обновление

После PR #959 другой инстанс обновил SHA и добавил ветки PR #958 и #959 в
PR #960. Этот коммит проверяет уже слитый PR #960 и удаляет его устаревшую
пометку «PR ещё не создан», чтобы инвентарь не выдавал завершённую ветку за
активную.

После PR #960 удалена дублирующая запись ветки `codex/lunobot2-branches-fix`,
добавлена ветка PR #962, а SHA инвентаря обновлён до текущего `main`.

После PR #963 ветка `codex/lunobot2-378-status` переведена из текущей работы
в слитые, а SHA обновлён до текущего `main`.
После PR #964 snapshot повторно обновлён до merge-коммита самого аудита.

После PR #965 SHA снова проверен по фактическому `main`; в корневой инвентарь
добавлены ветки PR #964 и #965, а PR #966 отмечен как текущая работа до его
слияния. После PR #966 ветка `codex/branches-audit-latest` переведена в
слитые, а текущей рабочей веткой стал `codex/lunobot2-420-status`.
После PR #967 ветка `codex/lunobot2-420-status` переведена в слитые, а
текущей рабочей веткой стала `codex/lunobot2-967-branches-status`. После PR
#968 эта ветка также переведена в слитые; активной текущей ветки в инвентаре
нет. После PR #970 в слитые добавлена ветка `codex/lunobot2-413-criteria`;
активной текущей ветки по-прежнему нет. После PR #971 в слитые добавлена
ветка `codex/branches-audit-after-968`; активной текущей ветки нет.
