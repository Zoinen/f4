# Test coverage — startup settings labels

Codecov показывает, что `cmd/f4/startup_settings.go` не имел покрытия.
Добавлен регрессионный тест для `startupBackendLabels`: автоматический пункт
локализуется, имена backend остаются идентификаторами, а результат не разделяет
память с входным списком.

Проверка выполняется GitHub CI; локальные Go-сборки и тесты не запускаются по
инструкции Лунобота.
