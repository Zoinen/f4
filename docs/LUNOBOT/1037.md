# Coverage: `InfoPanel` filesystem capacity display

## Status

Issue #1037 changes every reusable InfoPanel capacity meter to three explicit rows: total capacity centered above the percentage bar, the existing Space bar in the middle, and Used/Free values below it. Copying the meter row retains total, used, and free values.

The existing aligned-meter layout and rendering tests now pin the three-row structure, localized labels, byte-format toggle, percentage fill, and selected/unselected bar colors.

Local Go builds and tests were not run, per LUNOBOT.md § 10; GitHub Actions is the acceptance gate.

The corrective commit also restores the complete regression test in the branch; the acceptance gate remains GitHub Actions.

The branch now contains the complete three-row return layout and its regression test.

The alignment comment now documents the three-row layout explicitly.

The follow-up also restores the helper's original two-row scan; only the dedicated regression test uses the new three-row shape.
