# Лунобот-2 (instance a721a6d1487257292ae00780; LNX): тикет #607

## Статус

- [x] Полностью прочитаны тело тикета и вся доступная переписка; проверен
  связанный fix PR.
- [x] Исправление объединено в
  [f4#626](https://github.com/unxed/f4/pull/626), merge commit
  `f67c24a802168d4f94505218eac5abf7dde9f8c4`.
- [x] Заголовок пути активной панели теперь использует обычный нейтральный
  цвет и не создаёт второй визуальный указатель.
- [x] Цвет активной строки, курсор, рамки, вкладки и прочие состояния панели
  сохранены; регрессионная проверка охватывает обычный, активный и
  command-line-search варианты.
- [x] PR #626 прошёл полный GitHub CI и нативные проверки Windows/ARM64,
  включая ANSI PTY запуск.
- [ ] Пользовательская проверка и подтверждение автора ещё не получены; тикет
  не закрывается.

## Решения и инварианты

- Цвет пути в шапке не должен конкурировать с выделением текущего элемента на
  панели: активность панели не меняет этот цвет.
- Изменение касается только заголовка пути; курсор, выделенная строка,
  границы, вкладки и другие визуальные состояния не должны потерять прежнюю
  семантику.
- Отдельная логика в f4 не нужна: исправление уже находится в коде f4 и
  покрыто регрессионной проверкой.

## Проверка

- Локальные сборки и тесты не запускались согласно инструкции Лунобота.
- Статически проверены текущая история `main`, PR #626, версия `vtui` в
  `go.mod`, тело и комментарии тикета.
- Для пользовательской проверки нужно открыть две панели с разными путями и
  переключать фокус: цвет пути не должен меняться и создавать второй
  указатель, а активная строка файла должна по-прежнему выделяться.
