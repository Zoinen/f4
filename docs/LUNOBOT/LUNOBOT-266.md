# Лунобот-2: тикет #266, часть 2

## Статус

- [x] Прочитаны тело и вся переписка тикета.
- [x] Проверен PR #748: основная проблема массовой смены атрибутов уже исправлена и объединена.
- [x] Проверен оставшийся комментарий: в английском меню `Apply command` и `File attributes` конкурировали за mnemonic `A`.
- [x] Английский пункт `File attributes` перенесён на mnemonic `F`.
- [x] Добавлен регрессионный тест локализованной подписи действия.
- [ ] Проверены GitHub Actions.
- [ ] PR объединён в `main` и автору оставлен комментарий.

## Решение и инварианты

- Меняется только английская подпись `Action.File.Attributes`: `&File attributes`.
- Поведение действия и его настраиваемая клавиша `Ctrl+A` не меняются.
- Русская и остальные локализации не изменяются: в них нет описанного конфликта с английской подписью `Apply command`.
- Тикет не закрывается автоматически.

## Проверка

Локальные сборки и тесты не запускаются по инструкции Лунобота-2. До PR выполняется только `git diff --check`; окончательная проверка — через GitHub Actions.
