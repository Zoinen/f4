# Match group header interaction to its visible endpoint

The separator and text stopped before the scrollbar, but the hover rectangle,
MouseArea, and shared gallery header hit test still used the full header width.
A regression click two pixels before the scrollbar incorrectly collapsed the
group before this change.

Share the vertical scrollbar clearance with the separator, highlight width,
MouseArea right margin and scene-snapped pointer lookup endpoint. Columns and
non-scrolling panels preserve their previous bounds.

The regression now verifies that excluded clicks do not collapse, excluded
hover does not highlight, and ordinary header hover/click still work. Ten
Gallery results passed including pixel-grid and grouped pointer tests at DPR
1.75. Static QML imports and splitter tests passed. Rebuilt the static host,
embedded payload and portable launcher.

Keep both visual bounds and all input-routing paths synchronized when reserving
overlay clearance; narrowing only the MouseArea misses the shared pointer path.

`#group-header` `#hit-testing` `#hover` `#scrollbar`
