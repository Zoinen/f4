# Uniform View menu item heights

Group by had a separate 35-unit height while layout choices and Wide panel used
31. Introduce one pixel-snapped menuItemHeight shared by every actionable row in
the View menu and Group by submenu. Section headings and the Zoom control retain
their distinct heights.

The pre-fix regression failed with Group by height 35 versus regular height 31.
It now checks all enabled renderer rows against Group by and verifies a whole
physical-pixel height. Expanded visual coverage checks every affected title,
shortcut, heading, check and icon leaf's scene origin and unit transform. Give
those leaves explicit, single pixel-alignment corrections.

Menu geometry/hover tests passed with scale factors 1, 1.75 and 2; rendered output
was inspected, including small shortcuts. The hover fixture now waits for layout
and pointer departure before reopening the submenu. Existing zoom, ordering and
static QML import tests passed, as did git diff --check. Rebuilt the static host,
embedded payload and CGO-disabled portable launcher. No commit or push.

Keep actionable menu dimensions shared rather than assigning independent heights
to custom submenu rows. Preserve separate sizes only for non-action sections.
