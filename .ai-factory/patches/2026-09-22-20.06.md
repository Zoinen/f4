# Apply scrollbar clearance to group separator strokes

The preceding header-content fix intentionally left the line full-width, but
the requested clearance applies to the line as well. Subtract the visible
vertical scrollbar inset plus four pixels from the scene-snapped endpoint.
Without a vertical scrollbar, retain the full panel width.

The regression now inspects the actual separator's visual-tree item, failed
on its old endpoint, and passes after the change. Updated the previous
full-width assertion to reflect both scrollbar states. Ten Gallery test
results passed, including header pixel-grid coverage; static QML imports
passed. Inspected a DPR 1.75 rendered capture showing lines and counts ending
before the scrollbar. Rebuilt the embedded static host and portable launcher.

`#group-header` `#separator` `#scrollbar` `#regression-first`
