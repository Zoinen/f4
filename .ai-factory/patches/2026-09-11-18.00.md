# Settings native docking and category-switch work

- Reproduced footer at y=1161.71 in a 520 DIP dialog before the fix.
- Added typed owner layout/role metadata and SettingsDialogBody live docking.
- Removed outer scrolling; footer/search stay fixed, individual panes scroll.
- Compacted bordered group whitespace without modifying terminal geometry.
- Profile found Go ~0.419ms, Qt nested ListModel conversion and unused dropdown
  delegates. Replaced overlay ListModel with native incremental model and omitted
  duplicated external-popup choices; no page caches or throttling.
- Tests cover typed roundtrip, original owner immutability, dropdown open/selection,
  resize/scroll scene origins and unit vectors at DPR1.75, menu close/reopen/reorder.
- Rendered captures: .diagnostics/settings-categories/resize-after.png and
  actual-settings-after.png. Profile report and raw before/after logs alongside.
