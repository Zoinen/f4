version: "2"

linters:
  default: none
  enable:
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - gosec

  exclusions:
    generated: lax
    presets:
      - common-false-positives
      - legacy
